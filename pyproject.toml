[tool.poetry]
name = "rpi-clock"
version = "0.1.0"
description = "Raspberry Pi clock app scaffold"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = ">=3.9,<4.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
